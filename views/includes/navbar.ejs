<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
        </div>
        <div class="navbar-nav ms-auto" >
          <form action="/listings/search" method="GET" class="d-flex">
            <select name="locationCountry" class="search-inp">
                <option value="">Choose a location and country</option>
                <% locationsCountries.forEach(locationCountry => { %>
                    <option value="<%= locationCountry.location %>|<%= locationCountry.country %>">
                        <%= locationCountry.location %>, <%= locationCountry.country %>
                    </option>
                <% }) %>
            </select>
            <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
        
        </div>
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">StayAwhile your home</a>
          <% if(!currUser) { %>
            <a class="nav-link" href="/signup"><b>Signup</b></a>
            <a class="nav-link" href="/signin"><b>Signin</b></a>
          <% } %>
          <% if(currUser) { %>
            <a class="nav-link" href="/signout"><b>Signout</b></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>